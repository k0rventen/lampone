apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: iplookup
spec:
  interval: 24h
  chart:
    spec:
      chart: appchart
      version: 1.8.0
      sourceRef:
        kind: HelmRepository
        name: appchart
        namespace: flux-system
      interval: 1h
  values:
    apps:
      - name: iplookup
        image: gitea.cocointhe.cloud/coco/iplookup:0.2.0@sha256:4f9908f8b8dcfdb3fa1b5c6936da54d708318bce08bab18e3f13da8531459ad7
        service: 8000
        env:
          - name: IPINFO_TOKEN
            valueFrom:
              secretKeyRef:
                name: iplookup-secret
                key: IPINFO_TOKEN
        volumeMounts:
          - name: iplookup-data
            mountPath: /app/data
        volumes:
          - name: iplookup-data
            persistentVolumeClaim:
              claimName: iplookup-data
        pvc:
          - iplookup-data
